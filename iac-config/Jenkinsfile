pipeline {
    agent any
    
    tools {
      terraform 'Terraform'
      }

    stages {
        stage('Clean workspace') {
            steps {
             cleanWs()
            }
        }
        stage('Terraform init') {
            steps { 
                script {
                    dir('terraform') {
                    sh 'terraform init'
                    }    
                }
            }
        }
    }
}
